@model TravelBooking.Web.DTOs.Hotels.HotelDto
@{
    ViewData["Title"] = "Delete Hotel";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Delete Hotel</h1>
        <a asp-action="Index" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Back</a>
    </div>
    <div class="card shadow mb-4">
        <div class="card-header py-3 bg-warning">
            <h6 class="m-0 font-weight-bold text-dark">Confirm Delete</h6>
        </div>
        <div class="card-body">
            <p class="text-danger">Are you sure you want to delete this hotel? This action cannot be undone.</p>
            <dl class="row">
                <dt class="col-sm-3">Name</dt>
                <dd class="col-sm-9">@Model.Name</dd>
                <dt class="col-sm-3">City / Country</dt>
                <dd class="col-sm-9">@Model.City, @Model.Country</dd>
                <dt class="col-sm-3">Star Rating</dt>
                <dd class="col-sm-9">@Model.StarRating</dd>
                <dt class="col-sm-3">Price/Night</dt>
                <dd class="col-sm-9">@Model.PricePerNight.ToString("N2") @Model.Currency</dd>
            </dl>
            <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
                @Html.AntiForgeryToken()
                <button type="submit" class="btn btn-danger"><i class="fas fa-trash"></i> Delete</button>
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-info">Details</a>
            </form>
        </div>
    </div>
</div>
